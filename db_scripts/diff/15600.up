set autocommit=0;

use billing;

alter table ncos_lnp_list add column pattern varchar(255) not null;

# Setting all the current entries with pattern .* for backward compatibility
update ncos_lnp_list set pattern = '.*';

commit;